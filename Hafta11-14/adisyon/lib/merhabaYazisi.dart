import 'package:flutter/material.dart';

class MerhabaYazisi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Text(
      "Merhaba Dünya",
      style: TextStyle(fontSize: 20),
    );
  }
}
